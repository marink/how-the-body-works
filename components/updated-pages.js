/**
 * This module is used only when developing or writting content so that it can be
 * automatically refreshed when saving changes. 
 * 
 * The import of a specific page triggers the webpack hot module replacement (HMR)
 * which will update the page in the browser without a full reload.
 */

import "@public/content/glucose/conversion.html";
